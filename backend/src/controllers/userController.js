//


// Get user by ID
exports.getUserById = (req, res) => {
  return res.status(200).json({ message: "getUserById" });
};

// Get account owner data
exports.getUserData = (req, res) => {
  return res.status(200).json({ message: "getUserData" });
};

// Update user data
exports.updateUserData = (req, res) => {
  return res.status(200).json({ message: "updateUserData" });
};

// Delete user data
exports.deleteUserData = (req, res) => {
  return res.status(200).json({ message: "deleteUserData" });
};
